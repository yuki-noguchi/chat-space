.wrapper
  = render 'layouts/chat-menu'
  .chat-main
    .chat-main__header
      %h1
        = @group.name
      %h2
        Member :
        - @users.each do |user|
          = user.name
      %input.edit-btn{ type: :submit, value: "Edit" }
    = render 'layouts/chat-main'
