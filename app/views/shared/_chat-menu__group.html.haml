= link_to group_messages_path(group) do
  .chat-menu__group__box
    .group-name
      = group.name
    .group-message
      - if group.messages.present?
        = group.messages.last.body
      - else
        投稿がありません
